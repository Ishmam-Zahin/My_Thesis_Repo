# ====================== test.yaml ======================
model:
  model_path: "/home/cse/Desktop/zahin_thesis_work/My_Thesis_Repo/models/model8.py"
  model_class: "MyModel"
  vit_name: "dinov2_vits14"
  checkpoint_path: "/home/cse/Desktop/zahin_thesis_work/My_Thesis_Repo/checkpoints/model8/best_model.pth"
  vit_weight: null  # ← trained weights

data:
  json_root: "/home/cse/Desktop/zahin_thesis_work/My_Thesis_Repo/rearrange/dataset_json"
  dataset_names:          # ← you can test as many datasets as you want
    - "Celeb-DF-v1"
    - "Celeb-DF-v2"
    - "UADFV"   # add any other JSON filenames (without .json)
    - "DFDC"

paths:
  log_dir: "/home/cse/Desktop/zahin_thesis_work/My_Thesis_Repo/test_logs"   # same base as your train.yaml

training:
  batch_size: 6
  num_frames: 8          # MUST match the value you used during training
  knn_k: 8                # MUST match the value you used during training
  device: "cuda"

seed: 42                  # optional, for reproducibility